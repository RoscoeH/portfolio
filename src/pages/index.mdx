---
---

import theme from "../gatsby-plugin-theme-ui"

<SiteMetaData>
  {({ description }) => <Box sx={{ bg: "muted", p: 3 }}>{description}</Box>}
</SiteMetaData>

<Divider />

<SourceList>
  {projects =>
    projects.map(({ node: { frontmatter, fields } }, index) => {
      const { title, subtitle, description, links, featuredImage } = frontmatter
      const { slug } = fields
      return (
        <Card key={title} >
          <Flex mb="3">
            {featuredImage && (
              <Img
                fixed={featuredImage.childImageSharp.fixed}
                alt={`${title} icon`}
                imgStyle={{ borderRadius: theme.radii[4] }}
              />
            )}
            <Box ml="3">
              <Link href={slug} variant="heading">
                {title}
              </Link><br />
              <Text>{subtitle}</Text>
            </Box>
          </Flex>
          <Text>{description}</Text>{' '}
          <Link href={slug}>read more</Link>
        </Card>
      )
    })
}

</SourceList>
